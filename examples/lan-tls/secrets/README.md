# Secrets for the LAN/TLS example

Create the same three files here as for the top-level deployment
(`ops_key.txt`, `ops_secret.txt`, `server_token.txt`); see
[`secrets/README.md`](../../../secrets/README.md) at the repository root for
the commands. `.gitignore` keeps the `*.txt` files out of git.
