"""Installer package for wiring the bundled Agent Skill into host applications.

This package will grow an ``install()`` entry point in a later task; for now
it only exposes the bundled-Skill resolution helper in :mod:`.skill` and the
shared :class:`.errors.InstallerError` exception.
"""
